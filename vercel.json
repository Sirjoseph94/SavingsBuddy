{
  "version": 2,
  "buildCommand": "yarn deploy && yarn start",
  "builds": [
    {
      "src": "dist/index.js",
      "use": "@vercel/node",
      "config": { "includeFiles": ["dist/**"] }
    }
  ],
  "routes": [
    {
      "src": "dist/(.*)",
      "dest": "dist/index.js"
    }
  ]
}
